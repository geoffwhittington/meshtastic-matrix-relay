ignore:
  - tests/**/*

coverage:
  status:
    project:
      default:
        target: 40%
        threshold: 2%
    patch: off

comment:
  layout: header, diff, flags
  behavior: default
  require_changes: false
